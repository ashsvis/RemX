unit SimpleMemTest;

interface

implementation

//uses
//  Windows;

//var
//  msg: string;

//initialization

//finalization
//  if AllocMemCount-1 > 0 then
//  begin
//    Str(AllocMemCount-1, msg);
//    msg := msg + ' heap block left';
//    MessageBox(0, PChar(msg), 'Memory Leak',
//               MB_ICONERROR or MB_APPLMODAL or MB_OK);
//  end;
end.
